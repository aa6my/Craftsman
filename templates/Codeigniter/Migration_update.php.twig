<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Migration_{{name}} extends CI_Migration {

	public function __construct()
	{
		$this->load->dbforge();
		$this->load->database();
	}

	public function up() 
	{
		$this->update_{{table_name}}_table();
	}

	public function down() 
	{
		$this->db->truncate(ci_{{ table_name|replace({'update_':''}) }});
	}

	private function update_{{table_name}}_table()
	{
		$data = array(
		   array(
		      'field1' => 'value1',
		      # Set more fields
		   ),
		   # Add more if you like
		);		

		$this->db->update_batch('ci_{{ table_name|replace({'update_':''}) }}', $data, <id>); 
	}	
}

/* End of file {{filename}}.php */
/* Location: {{path}} */